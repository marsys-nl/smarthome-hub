ktor:
    deployment:
        port: ${ENV_PORT:8080}

smarthome:
    auth:
        apiKey: ${ENV_AUTH_API_KEY:}

        keycloak:
            issuer: ${ENV_AUTH_KEYCLOAK_ISSUER:}
            jwk: ${ENV_AUTH_KEYCLOAK_JWK:}
